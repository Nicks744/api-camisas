# URL de conex�o com o banco de dados
spring.datasource.url=jdbc:mysql://localhost:3306/camisa_time?useSSL=false&serverTimezone=UTC

# Usu�rio e senha para conex�o com o banco
spring.datasource.username=root
spring.datasource.password=root

# Configura��o do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Exibir SQLs no log (opcional, mas �til para depura��o)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
